

//- script(type='text/javascript').
//-     window.onload=function(){document.getElementById("yukleniy").style.display="none";} 

<!doctype html>
html
    //- style(type='text/css').
    //-     div.hbne{opacity:0.7;filter:alpha(opacity=90);position:fixed;z-index:4000;}
    //-     div.hghd{top:0px;left:0px;}
    //-     div.hbne{_position:absolute;}
    //-     div.hghd{_bottom:auto;_top:expression
    //-     (ie6=(document.documentElement.scrollTop+document.documentElement.clientHeight - 52+"px") );}
    include ../includes/head.pug
    link(href='/css/themes/services/style.css' rel='stylesheet')
    //- p#tawkto
    //- each systems in system
    //-     p !{systems.tawktoscript}

    body.hero-anime
        include ../includes/navbar.pug
        style.
            .float{
            position:fixed;
            width:60px;
            height:60px;
            bottom:30px;
            left:30px;
            background-color:#25d366;
            color:#FFF;
            border-radius:50px;
            text-align:center;
            font-size:30px;
            box-shadow: 2px 2px 3px #999;
            z-index:100;
            }
            .my-float{
            margin-top:16px;
            }
        a#whatsapp.float(href='https://api.whatsapp.com/send?phone=' target='_blank')
            i.fa.fa-whatsapp.my-float

        //- #yukleniy.hbne.hghd(style='background-color:#ffffff;width:100%;height:100%;padding-top:20%;')
        //-     center
        //-         a(style='font-family:Arial;font-size:19px;color:#4f4f4f;') Sayfa Y&uuml;kleniyor
        //-         br
        //-         br
        //-         a(target='_blank' style='background-color:transparent;border:none;padding:0px;margin:0px;')
        //-             img(style='padding:0px;margin:0px;background-color:transparent;border:none;' src='/img/1497.gif' _fcksavedurl='/img/1497.gif')
        script.
            $(document).ready(function() {
                    
                $.ajax({
                
                url: '/api/logo',

                type: 'GET',
                async:false,
                
                data: { format: 'json' },
                
                success: function(data) {
                result=JSON.stringify(data.system[0].tawktoscript);
                tawkto=JSON.parse(result);
                $('head').append(tawkto)
                revizeverim=JSON.stringify(data.logo.favico);
                logo=JSON.parse(revizeverim);

                result=JSON.stringify(data.system[0].phone);
                phone=JSON.parse(result);
                $("a#whatsapp").attr(`href`,`https://api.whatsapp.com/send?phone=${phone}`);
                },
                
                });
                var link = document.querySelector("link[rel*='icon']") || document.createElement('link');

                link.type = 'image/x-icon';
                link.rel = 'shortcut icon';
                link.href = `/img/${logo}`;
                document.getElementsByTagName('head')[0].appendChild(link);
            });


        block content

        include ../includes/footer.pug
        include ../includes/script.pug
        script(src="/js/pages/services/main.js")
